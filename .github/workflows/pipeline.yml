name: pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  buildx:
    uses: guilhermelinosp/ci-templates/.github/workflows/buildx.yaml@latest
